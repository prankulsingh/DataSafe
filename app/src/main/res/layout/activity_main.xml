<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="in.ac.iiitd.prankul.datasafe.MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="70dp"
        android:text="Shared Preferences"
        android:id="@+id/shared_preferences"
        android:layout_marginBottom="49dp"
        android:layout_above="@+id/files"
        android:layout_centerHorizontal="true"
        android:onClick="clickSharedPreferences" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="70dp"
        android:text="Database"
        android:id="@+id/database"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/files"
        android:layout_alignStart="@+id/files"
        android:layout_alignEnd="@+id/files"
        android:onClick="clickDatabase" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="70dp"
        android:text="Files"
        android:id="@+id/files"
        android:onClick="clickFiles"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/shared_preferences"
        android:layout_alignEnd="@+id/shared_preferences" />
</RelativeLayout>
